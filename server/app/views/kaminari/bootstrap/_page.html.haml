- if page.current?
  %li.page-item.active
    = content_tag :a, page, remote: remote, rel: page.rel, class: 'page-link'
- else
  %li.page-item
    = link_to page, url, remote: remote, rel: page.rel, class: 'page-link'
